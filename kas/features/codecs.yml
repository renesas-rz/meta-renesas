header:
  version: 14

repos:
  # Needs to be manually added to the working directory
  meta-rz-features:
    path: meta-rz-features
    layers:
      meta-rz-codecs:

local_conf_header:
  license_flags_codecs: |
    LICENSE_FLAGS_ACCEPTED += "commercial_gstreamer1.0-omx"
