# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILES_DYNAMIC += " \
    rz-graphics:${LAYERDIR}/dynamic-layers/rz-graphics/*/*.bb \
    rz-graphics:${LAYERDIR}/dynamic-layers/rz-graphics/*/*.bbappend \
"

BBFILE_COLLECTIONS += "rzv2n"
BBFILE_PATTERN_rzv2n = "^${LAYERDIR}/"
BBFILE_PRIORITY_rzv2n = "15"

LAYERDEPENDS_rzv2n = "rz-common rzg2l rzv2h"
LAYERSERIES_COMPAT_rzv2n = "dunfell"
